import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
  return (
    <Base title="UserDashboard page">
      <h1 className="text-white">User Dashboard haeding</h1>
    </Base>
  );
};

export default UserDashboard;
